{
  "name": "notion-clone-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.0.0",
  "workspaces": ["backend", "frontend"],
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "build": "pnpm -w --filter ./backend... run build && pnpm -w --filter ./frontend... run build",
    "dev": "pnpm -C backend run _dev & pnpm -C frontend run _dev"
  }
}
